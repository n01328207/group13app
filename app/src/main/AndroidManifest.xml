<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group13app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group13app">
        <activity android:name=".communicationEmail"></activity>
        <activity android:name=".Payment2" />
        <activity android:name=".Payment1" />
        <activity android:name=".Security" />
        <activity android:name=".myorder3" />
        <activity android:name=".myorder2" />
        <activity android:name=".myorder1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>